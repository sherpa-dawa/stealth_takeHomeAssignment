import { Typography } from '@mui/material';

export default function Home() {
  return (
    <main style={{ padding: '2rem' }}>
      <Typography variant="h1">Audit Planning Workspace</Typography>
    </main>
  );
}
